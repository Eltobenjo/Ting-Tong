return (
    <div className="ob">
      {berlin.dishes.map((item) => (
        <button
          onClick={handleClick}
          id={item.id}
          data-price={item.price}
          className="orderButton"
        >
          <span>
            {item.name} {""}€{item.price}
          </span>
        </button>
      ))}
    </div>
  );
}